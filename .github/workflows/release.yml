name: Release

on:
  push:
    tags:
      - 'v*'  # Trigger on version tags like v1.0.0

  # Allow manual trigger
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., v1.0.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-release:
    name: Build Release Artifacts
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: macos-latest
            target: macos-arm64
            pkg-target: node20-macos-arm64
            binary-name: aio-macos-arm64
          - platform: macos-latest
            target: macos-x64
            pkg-target: node20-macos-x64
            binary-name: aio-macos-x64
          - platform: ubuntu-latest
            target: linux-x64
            pkg-target: node20-linux-x64
            binary-name: aio-linux-x64
          - platform: windows-latest
            target: windows-x64
            pkg-target: node20-win-x64
            binary-name: aio-windows-x64.exe

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build TypeScript
        run: npm run build

      - name: Obfuscate backend code
        run: npm run obfuscate -w orchestrator-backend

      - name: Build web frontend
        run: npm run build:web

      - name: Copy web assets into backend for embedding
        run: cp -r orchestrator-web/dist orchestrator-backend/web-dist
        shell: bash

      - name: Install pkg
        run: npm install -g pkg

      - name: Create dist directory
        run: mkdir -p dist

      - name: Build backend binary with embedded assets
        run: cd orchestrator-backend && pkg . --targets ${{ matrix.pkg-target }} --output ../dist/${{ matrix.binary-name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.binary-name }}
          path: dist/${{ matrix.binary-name }}
          retention-days: 1

  create-release:
    name: Create GitHub Release
    needs: build-release
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Prepare release files
        run: |
          mkdir -p release
          # Copy binaries to release folder
          cp artifacts/aio-macos-arm64/aio-macos-arm64 release/
          cp artifacts/aio-macos-x64/aio-macos-x64 release/
          cp artifacts/aio-linux-x64/aio-linux-x64 release/
          cp artifacts/aio-windows-x64.exe/aio-windows-x64.exe release/
          # Make Unix binaries executable
          chmod +x release/aio-macos-arm64 release/aio-macos-x64 release/aio-linux-x64
          ls -la release/

      - name: Get version
        id: version
        run: echo "version=${{ github.event.inputs.version || github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.version.outputs.version }}
          name: AIO Orchestrator ${{ steps.version.outputs.version }}
          draft: true
          prerelease: false
          files: |
            release/aio-macos-arm64
            release/aio-macos-x64
            release/aio-linux-x64
            release/aio-windows-x64.exe
          body: |
            ## AIO Orchestrator ${{ steps.version.outputs.version }}

            Multi-agent orchestrator for Claude Code.

            ### Installation

            **macOS (Apple Silicon):**
            ```bash
            curl -L -o aio https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/aio-macos-arm64
            chmod +x aio
            ./aio
            ```

            **macOS (Intel):**
            ```bash
            curl -L -o aio https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/aio-macos-x64
            chmod +x aio
            ./aio
            ```

            **Linux (x64):**
            ```bash
            curl -L -o aio https://github.com/${{ github.repository }}/releases/download/${{ steps.version.outputs.version }}/aio-linux-x64
            chmod +x aio
            ./aio
            ```

            **Windows (x64):**
            Download `aio-windows-x64.exe` and run it.

            **Homebrew (coming soon):**
            ```bash
            brew install FurlanLuka/tap/aio
            ```

            ### Usage

            ```bash
            aio                    # Start server, open browser
            aio --port 8080        # Use specific port
            aio --no-browser       # Don't open browser
            ```

            ### Features

            - **Single binary** - Web UI and setup files are embedded
            - **Auto port** - Finds free port starting from 3456
            - **Auto browser** - Opens browser automatically (use --no-browser to disable)

            ### Requirements

            - Claude Code CLI must be installed: `npm install -g @anthropic-ai/claude-code`
            - Run `claude auth` to authenticate before using AIO Orchestrator

            ### Changes

            See the [full changelog](https://github.com/${{ github.repository }}/commits/${{ steps.version.outputs.version }}) for details.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  update-homebrew:
    name: Update Homebrew Tap
    needs: build-release
    runs-on: ubuntu-latest
    if: "!contains(github.event.inputs.version || github.ref_name, '-')"  # Skip for pre-releases

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          mkdir -p release
          cp artifacts/aio-macos-arm64/aio-macos-arm64 release/
          cp artifacts/aio-macos-x64/aio-macos-x64 release/
          cp artifacts/aio-linux-x64/aio-linux-x64 release/
          chmod +x release/*
          ls -la release/

      - name: Calculate SHA256 checksums
        id: sha
        run: |
          echo "macos_arm64=$(sha256sum release/aio-macos-arm64 | cut -d' ' -f1)" >> $GITHUB_OUTPUT
          echo "macos_x64=$(sha256sum release/aio-macos-x64 | cut -d' ' -f1)" >> $GITHUB_OUTPUT
          echo "linux_x64=$(sha256sum release/aio-linux-x64 | cut -d' ' -f1)" >> $GITHUB_OUTPUT

      - name: Checkout Homebrew tap
        uses: actions/checkout@v4
        with:
          repository: FurlanLuka/homebrew-aio
          token: ${{ secrets.HOMEBREW_TAP_TOKEN }}
          path: homebrew-tap

      - name: Get version
        id: version
        run: echo "version=${{ github.event.inputs.version || github.ref_name }}" >> $GITHUB_OUTPUT

      - name: Create release in homebrew-aio repo
        uses: softprops/action-gh-release@v1
        with:
          repository: FurlanLuka/homebrew-aio
          tag_name: ${{ steps.version.outputs.version }}
          name: AIO ${{ steps.version.outputs.version }}
          files: |
            release/aio-macos-arm64
            release/aio-macos-x64
            release/aio-linux-x64
        env:
          GITHUB_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}

      - name: Update formula in tap
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          VERSION_NUM="${VERSION#v}"  # Remove 'v' prefix

          mkdir -p homebrew-tap/Formula

          cat > homebrew-tap/Formula/aio.rb << 'FORMULA'
          class Aio < Formula
            desc "Multi-agent orchestrator for Claude Code"
            homepage "https://github.com/FurlanLuka/homebrew-aio"
          FORMULA

          cat >> homebrew-tap/Formula/aio.rb << EOF
            version "${VERSION_NUM}"
            license "MIT"

            on_macos do
              on_arm do
                url "https://github.com/FurlanLuka/homebrew-aio/releases/download/${VERSION}/aio-macos-arm64"
                sha256 "${{ steps.sha.outputs.macos_arm64 }}"
              end
              on_intel do
                url "https://github.com/FurlanLuka/homebrew-aio/releases/download/${VERSION}/aio-macos-x64"
                sha256 "${{ steps.sha.outputs.macos_x64 }}"
              end
            end

            on_linux do
              on_intel do
                url "https://github.com/FurlanLuka/homebrew-aio/releases/download/${VERSION}/aio-linux-x64"
                sha256 "${{ steps.sha.outputs.linux_x64 }}"
              end
            end

            def install
              if OS.mac?
                if Hardware::CPU.arm?
                  bin.install "aio-macos-arm64" => "aio"
                else
                  bin.install "aio-macos-x64" => "aio"
                end
              elsif OS.linux?
                bin.install "aio-linux-x64" => "aio"
              end
            end

            def caveats
              <<~EOS
                To get started, run:
                  aio

                This starts the orchestrator and opens http://localhost:3456 in your browser.

                Options:
                  aio --port 8080     # Use specific port
                  aio --no-browser    # Don't open browser automatically
                  aio --help          # Show all options

                Requirements:
                  Claude Code CLI must be installed:
                  npm install -g @anthropic-ai/claude-code
              EOS
            end

            test do
              assert_match "version", shell_output("\#{bin}/aio --help")
            end
          end
          EOF

      - name: Commit and push to tap
        run: |
          cd homebrew-tap
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/aio.rb
          git diff --staged --quiet || git commit -m "Update aio to ${{ github.event.inputs.version || github.ref_name }}"
          git push
